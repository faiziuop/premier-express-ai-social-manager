Premier Express AI Social Manager v4
Secure-login version.

Deploy these files to Vercel. The dashboard uses Supabase Auth. Create/sign in with a private dashboard account. The AI Edge Function requires an authenticated Supabase user.

IMPORTANT: Use only the Supabase Publishable/Anon key in the browser. Never use a Secret/Service-Role key. OpenAI remains a Supabase secret.

After deployment: sign up, confirm email if Supabase asks, sign in, then Generate today's 5 posts.
